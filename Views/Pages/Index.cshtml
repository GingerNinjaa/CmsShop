@model CmsShop.Models.ViewModels.Pages.PageViewModel

@{
    ViewBag.Title = ViewBag.PageTitle;
    // podświetlanie PageTitle part 1/2
    TempData["PageTitle"] = ViewBag.PageTitle;
}

@Html.Raw(Model.Body)

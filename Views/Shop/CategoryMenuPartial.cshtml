@model IEnumerable<CmsShop.Models.ViewModels.Shop.CategoryViewModel>

<h3>Kategorie</h3>

<ul>
    @foreach (var item in Model)
    {
        <li>
            <a href="/shop/category/@item.Slug.ToLower()">@item.Name</a>
        </li>
    }
</ul>